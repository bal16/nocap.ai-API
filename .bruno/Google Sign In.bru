meta {
  name: Google Sign In
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/sign-in/social
  body: json
  auth: inherit
}

body:json {
  {
    "provider": "google"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
